# Optional: override Postgres credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=neonlead

# Optional: search API keys (pick one)
# BING_SEARCH_KEY=
# SERPAPI_KEY=

# Optional: rotating proxies (comma or newline separated)
# PROXY_LIST=http://user:pass@ip1:port,http://user:pass@ip2:port
